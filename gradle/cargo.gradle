apply plugin: 'cargo'

cargo {
    containerId = 'tomcat7x'
    port = 8080

    deployable {
        context = "/"
    }

    remote {
        hostname= '127.0.0.1'
        username = 'arahansa'
        password = 'arahansa'
    }
}